{
  "name": "amethyst-dragon",
  "version": "0.0.1",
  "description": "Welcome discord bot",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p .",
    "prestart": "npm run build",
    "start": "node dist/main",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ScuroGuardiano",
  "license": "AGPL-3.0-or-later",
  "dependencies": {
    "canvas": "^2.8.0",
    "discord.js": "^13.2.0",
    "dotenv": "^10.0.0"
  }
}
